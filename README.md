Modatna (Arabic for "Fashion that's Ours") is a Streamlit App exploring fashion resale value and style archetypes using real user data.
How to Run: 
```bash
pip install -r requirements.txt
streamlit run fas.py
